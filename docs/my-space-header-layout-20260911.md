# 我的空间顶部排版调整

2026-09-11，按 FC Signal 赛事中心实现规范 v2.4 调整。本次范围是我的空间及已登录我的关注的账号标题、参赛上下文和功能导航。

- 标题区收紧；账号身份、设置及邮箱验证入口归入账号菜单。邮箱状态未同步时不推断为未验证。
- 当前赛事、状态、队伍及对应身份集中排列；只有存在多项参赛赛事时才提供切换入口。
- 功能导航移除重复设置入口，统一高度，以黄色下划线标记当前大类，展开控件改用箭头。
- 进入参赛或多身份工作台分组后，直接展示子栏目和当前页面。手机端折叠一级栏目，保留当前子栏目。
- 顶部菜单支持 Esc 关闭并返回触发器、点击外部关闭及键盘移出关闭；页面跳转继续使用已有草稿保护。

本机验证：

- 浏览器覆盖 2048、1280、980、390px；补查 320px 英文关注页。外层文档未出现横向溢出。980px 英文一级导航保持单行。
- 宽屏概览顶部区域约 185px；1280px 带参赛子导航的顶部约 226px，均不含全站导航栏。
- 验证参赛子页切换、账号菜单、邮箱验证跳转、手机菜单、Esc 与外部关闭。修改样例名单后点击新子导航会出现离开确认，取消保留修改，撤销修改后可正常切换。
- 核对选手只读栏目、未关联账号入口及已登录关注页；样例单赛事不显示切换按钮。
- `npm run test:my-space`、`node scripts/assertAccountCompetition.mjs`、`node scripts/assertAccountReview.mjs`、修改组件的 ESLint 通过。隔离目录 Vite 生产构建通过（11.60 秒）。

页面总览：[本机账号页面审阅](http://127.0.0.3:3047/dev/account-review?page=overview)。使用本机样例会话；本轮未发布，也不代表其余工作台内容已完成重新设计或生产验收。多赛事切换的数据规则由现有回归覆盖，本轮没有在浏览器构造多赛事账号。
