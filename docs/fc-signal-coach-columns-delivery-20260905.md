# 比赛详情：左右对照与信息密度调整

日期：2026-09-05。状态：本地可运行设计稿。

[打开第一张地图](http://127.0.0.1:3026/matches/FCR26-PLAYOFFS-R1-M13?design=kpr5&lang=zh&season=FCR2026&map=1)。本次承接教练逐图战报，针对宽屏下指标过于分散、双方需要上下滚动才能对照的问题调整数据区。

## 当前呈现

- 桌面每张图左 A 队、右 B 队。两边列宽、行高和职责顺序一致，同时查看两队的五名选手。
- 消灭、助攻、阵亡合为一个数值组，仍分别保留原值；伤害、治疗、阻挡和评分保留独立列。
- 禁用、队名与本图比分放入各队表头，英雄头像、姓名和职责放在对应数据行。默认展开时去掉上方重复的阵容条；收起数据时显示阵容摘要。
- 同职责的起始行对齐，各队职责内仍使用原记录顺序。若一方有更多英雄记录，另一方使用不计入统计的空行保持后续职责对齐；没有强行合并或删去记录。
- 保留本图最高数值高亮、队伍合计及选手档案链接。原有关键表现文字保留在选手提示中，减少表内重复标签。
- 内容宽度不足 960px 时上下排列两队，保持完整指标可读。手机表格最小宽度由 790px 收到 480px，姓名列继续固定。

## 本轮验证

| 检查 | 结果 |
| --- | --- |
| 1440px 桌面 | 双列各约 577px；两边每行 65px，职责行起点一致，全部指标直接显示 |
| 1280px 笔记本 | 双列各约 503px；未发现数值裁切或内部横滚 |
| 390px 手机 | 各队表格容器约 357px，表格 480px；实际横滚约 123px 后姓名列仍固定，文档无外层溢出 |
| 英文 | 1280px 的双列标题、禁用、英雄和指标正常显示 |
| 数据 | M13 五图、50 条选手记录、300 个数值逐项与原始公开记录一致；十组队伍合计一致 |
| 折叠 | 收起第一图显示十位出场选手的摘要，第二图保持展开；可重新展开 |
| 选手返回 | 从英文双列的 NONG 数据行进入选手档案，返回同一地图、语言与阅读位置 |
| 原版与跨赛事 | 原版仍渲染原有两个队伍表；QGCS4 的 NF–SK 弃权场仍显示 3:0 与弃权说明，没有实赛统计 |
| 工程 | 定向 ESLint 和空白检查通过；Match Review 7 项、Signal Batch 2 9 项通过；独立 Vite 构建 997 模块、18.89s 通过 |

备份与独立构建位于 `artifacts/fc-signal-coach-columns-20260905/`。本轮没有修改评分模型、源数据、原版数据表或发布设置，也不代表全站上线验收。公开样本内每图双方职责记录数相同，额外记录的占位对齐通过实现检查，尚无真实不对称样本的浏览器验证。
