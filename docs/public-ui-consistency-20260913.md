# 六个主页面公共框架统一精修

日期：2026-09-13。范围：本地 FC Signal 候选版；未发布。

## 本轮问题与调整

此次从六个主页面横向检查公共区域，保留各页已经建立的内容表达。归档总览、人物与阵容主视觉继续使用 Archive 密度；表格和排行保留紧凑阅读密度。

- 周赛总览原有通栏数据通知、页内身份条和页尾更新时间分散；公开关注页重复出现页面名称与数据通知。现在与赛程、阵容、排行共用一条赛事状态栏。归档晋级页也使用同一状态栏，原有路线场景继续保留。
- 演示数据说明集中在状态栏，更新失败或备用快照提示仍保留各自原文。生产数据不会被标记为演示；已登录账户的其他工作区不进入公开关注页的规则。
- 阵容与数据分区、周次、关注视图共用 SignalSectionNav 的文本、分隔线、黄色选中线、悬停与键盘焦点。普通单行导航桌面最小高 56px、手机 48px；带状态的手机周次最小高 64px，窄屏统一把状态放在周次下方。
- 普通索引标题统一到 30–46px；紧凑数据标题保留 28–38px 档位。字号、行高与常用标题留白进入公共变量，关注页恢复与其他索引一致的标题字重。
- 周期选择使用公共颜色变量、44px 点击区域和稳定的下边线，不再依赖各页面的局部颜色别名。周赛搜索图标与排行统一，搜索和队伍筛选高 48px；手机周期和搜索使用 16px 文字。阵容总览手机搜索同时修正。
- 控件的公共圆角变量统一为 0，遵循已确认的直角方向。

没有修改比赛、积分、评分、名单或关注存储模型。没有新增筛选标签行，也没有调整已确认的全局悬浮页头构图。

## 本地验证

- 六个主页面检查 1440px 中文、980px 英文、390px 中文；周赛总览、赛程、晋级和关注另查 1280px，关注、总览与阵容补查 320px 英文。所检页面文档外层无横向溢出。周次、阵容导航和数据表允许各自必要的内部滚动。
- 实际浏览器检查了关注视图切换、试运行周期与一条相关比赛、赛程搜索空结果与重置、英文第 4 周切换，以及从周期选择 Tab 到来源入口时的可见焦点。周期选择高 44px，周赛搜索与队伍筛选高 48px。
- FCR2026 归档晋级页保留原路线主视觉；QGCS4 地图页正常显示其赛事状态与数据导航。原版排行榜没有新版导航或紧凑状态栏泄漏。
- 现有检查共 75 项通过：数据来源状态 4、周赛总览 18、赛程 19、晋级 18、周赛关注 10、比赛阅读状态 6。修改 JSX 的定向 ESLint 通过。
- 最终 Vite 构建使用独立输出目录，绕过会生成公共数据的 prebuild。任务范围内空白检查与构建结果保存在下述任务目录。

周赛浏览器数据来自明确标注的本地演示快照；FCR2026 使用已有本地公开记录，QGCS4 读取其公开数据。浏览器观察到初始加载、正常数据来源及后台更新；更新失败和备用来源语义由现有测试覆盖，本轮未人为制造生产网络故障。未执行真实账户写入、System 联调或部署。

修改前副本、修改后副本、精确任务差异、构建日志及验证记录：`tmp/public-ui-consistency-20260913/`。
