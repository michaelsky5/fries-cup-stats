# 竞技分析：修正长短栏造成的大块空白

用户指出「英雄与阵容」下部右侧有大面积空白。本轮在 3040 预览复现：等宽两栏把较长的阵容记录和较短的禁用列表并排，1920 像素视口中右栏内容比左栏早结束约 585 像素。

## 调整

- 阵容与禁用改为上下独立区域。
- 阵容在宽屏改为横向记录行，将完整记录占比、五英雄、战绩、常见地图及证据入口放在同一行。证据展开后占据整行，后续组合紧接其下。
- 窄桌面将阵容背景移到下一行；手机仍按占比、五英雄、地图背景、入口逐层阅读。地图名使用独立文本单元，优先完整换行。
- 禁用列表横向占满内容区，保留本队／对手切换，加入按当前最大禁用图数等比例显示的辅助条；条形不代表胜率或针对程度。
- 成员专题的可比地图证据移到双栏下方，避免展开大量地图后只拉长右栏。额外出场组合也移到整行。
- 分析计算、样本门槛、阵容排序、证据 ID 和 URL 状态逻辑不变。

## 本地验证

- 1920 × 1000 中文：阵容区域约从 1079 缩至 556 像素；默认三条阵容记录各约 123 像素。禁用列表紧随其下，无半栏空洞。
- 1440 × 1000 中文：阵容行、地图名称、禁用入口可读，没有页面横向溢出。
- 1024 × 900 英文：五英雄、背景信息和证据按钮顺序清晰，无页面横向溢出。
- 390 × 844 中文：五英雄可见，证据入口宽约 358、高约 53 像素；禁用切换和展开正常，无页面横向溢出或已加载的损坏图片。
- AIP：全部 26 种阵容仍可展开；第一组显示 3 张真实地图；本队／对手禁用切换正常，分别核对了首组 4 张地图。
- AIP / EVER：32 张减伤可比地图在成员双栏下方展开，证据与内容区同宽约 1126 像素。
- FCR26-T006：额外 8 组出场组合在整行展开，不再拉长单侧。
- MNW：无阵容及禁用记录时仍为空态，没有补出统计内容。
- 本轮末尾控制台 error / warn 返回空列表。

定向 ESLint、31 项分析／阵容／阅读状态测试、直接 Vite 构建通过。只更新本地预览，未提交或发布。

预览：`http://127.0.0.1:3040/teams/FCR26-T026/analysis?design=kpr5&lang=zh&season=FCR2026&analysisView=full&analysisTopic=heroes`

修改前副本和哈希保存在 `tmp/team-analysis-layout-20260911/`；完成后的文件哈希见 `result-manifest.json`。3039 预览保留。
