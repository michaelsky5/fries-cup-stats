# 参赛阵容本地设计收尾

日期：2026-09-11。范围：`design=kpr5` 的阵容总览、队伍目录与职员目录衔接。本批保留已经确认的选手目录共同框架、五项导航和现有素材方向，完成本地收尾；未提交或发布。

## 本次完成

- 队伍目录补齐“季后赛加入／季后赛退出”。总览和队伍目录共用 `rosterStage.js` 的公开字段判断与中英文文案。桌面在姓名旁显示轻量注记；手机将职责与注记置于姓名下方。保留全季历史名录、长姓名和档案入口，不截断累计人数。
- 赛事职员默认按已发布比赛署名场数降序，场数相同按姓名、记录 ID 稳定排序。筛选保留手动选择的昵称或职务顺序；清除全部后恢复默认。战队职员和原版综合职员目录保留职务顺序。战队职员链接中的不适用署名排序会被清理。
- 从队伍目录进入选手档案后，返回按钮改为“返回参赛队伍 / Back to teams”，与真实返回路径一致。原有队伍、查询、赛季、语言和展开状态继续恢复。
- [赛事中心设计规范](fries-cup-data-signal-design-system.md) 更新至 2.5.2，纳入上述规则及已验证的[名单联动英雄](fc-signal-roster-hero-trial-20260911.md)。

## 验证

- `node --test scripts/assertTeamIndex.mjs scripts/assertStaffDirectoryGroups.mjs scripts/assertRosterOverview.mjs`：30/30 通过。其中 ECNU 的九人历史名录与四处变更注记在两个入口完全一致；未知阶段不推断变更；署名排序、角色筛选和旧链接兼容通过。
- 修改的 JS / JSX 文件通过 ESLint；跟踪文件通过 `git diff --check`。相对修改前备份的差异只包含本次注记、排序、返回文案、测试和文档。
- 直接 Vite 隔离构建通过，10.13 秒。输出为 `tmp/roster-finish-20260911-170727/build`，日志为同目录的 `build.log`；只有已有的 CSS 插件耗时提示。未调用会重新生成公共数据的 npm 前置脚本。
- 实际浏览器检查 2048、1280、390 px：ECNU 中文与英文注记、BR 英文长姓名均完整，没有外页横向溢出。键盘进入 -DingChunQiu- 档案后，修正的返回按钮恢复 BR、搜索条件、英文和手机展开状态。
- FCR2026 赛事职员默认依次为 Maverick 27、照井龙 24、牛萨库斯 21、南宫、20；赛管筛选保持降序；手选昵称后切换解说仍保留昵称排序；清除全部恢复署名排序。第二页继续从 3 场、2 场、1 场向下排列。中英文手机排序控件可读。
- 战队职员默认和原版综合目录的三个排序选项保持原有行为。QGCS4 赛事职员同样按署名场数降序，缺少已发布回顾时没有虚构回顾入口。
- 总览 ECNU 仍保留两处加入、两处退出注记，默认英雄仍对应 3e。检查标签页与总览没有应用控制台错误。

修改前文件保存在 `tmp/roster-finish-20260911-170727/before/`，清单为 `files.json`，相对备份的差异为 `changes.patch`。原有本地预览继续运行于 `http://127.0.0.1:3026/roster?design=kpr5&lang=zh&season=FCR2026`；本批实现与验证完成，正式发布另行执行。
