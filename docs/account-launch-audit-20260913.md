# 账号系统上线复查 · 2026-09-13

结论：当前实现可进入前后端联合测试部署验收，尚不能认定可直接供正式周赛使用。本次只检查并生成证据，没有修改产品代码、发送邮件、操作生产账号或执行部署。

用户路线：受邀认领 → 登录 → 我的空间及周赛入口 → 资料与会话管理 → 密码恢复。生产数据由 System 提供；47 个本机页面与状态入口不等于 47 条生产流程已通过。

## 当前阻塞与证据

| 项目 | 观察与影响 | 下一步 |
| --- | --- | --- |
| 正式账号入口未发布 | 11:04 SGT 左右访问 `https://stats.fries-cup.com/account`，HTTP 为 SPA 的 200，但浏览器实际显示 `Unexpected Application Error! / 404 Not Found`。正式入口仍使用 `/assets/index-DJZWOBbd.js` | 发布包含当前账号路由的固定 Stats 候选，验收直接进入与刷新 |
| 正式账号 API 转发未接通 | 正式 Stats 的 `/api/platform/auth/config`、`/auth/me`、`/me/identities` 及 health 均返回 `200 text/html`，内容是同一 SPA 文档，不是 JSON。配置应为 JSON；未登录身份接口应为受保护的未认证响应 | 将工作区 `vercel.json` 的账号转发与对应 System 版本一起验收；进一步验证 Cookie、Origin 与缓存边界 |
| 发布候选未覆盖最新账号实现 | 当前公开 UI 候选声明只验收公开页面，且其 25 个相关输入中有 8 个文件的摘要已不同于工作区。抽查 AuthDialog、邀请认领、账号设置，忽略行尾后仍有代码差异 | 冻结新的两端候选及生产配置，对同一个固定包完成测试，避免直接发布旧候选或混合工作区 |
| 真实邮件与域名会话未验收 | 本轮未发送真实邮件；线上账号代理在前一边界已经失败，因此未继续模拟其后成功。无法据此判断生产邮件是关闭、缺配置还是已经可投递 | 在测试部署验证邀请、邮箱验证、密码找回的真实送达与正确跳转；确认服务端设置与状态回读 |
| 完整发布与回滚仍需验收 | System 工作区含 Compose 与 Prisma schema 改动。当前自动部署流程明确拒绝未纳入维护流程的 Compose 变更；proxy 单项允许参数不覆盖 Compose。脚本静态检查通过不代表最新生产备份、数据库升级与恢复已经执行 | 固定后端范围；在隔离目标副本完成升级与应用回滚，保存当前生产版本、配置及可读备份，按对应维护路径发布 |

这里区分“线上尚未发布”和“候选发布验收未完成”：前两项描述当前正式站；后三项说明不能直接将已有本机包视为获准发布的完整账号版本。

## 本轮实际通过

- `npm run test:account-ui`：通过，包括邀请、账号恢复、会话、权限、我的空间、周赛接口边界。
- 当前源码 ESLint：0 错误、14 条已有 Hook 警告，位于经理商店和比赛房文件。最初 `npm run lint` 会扫描未排除的本机 `.codex-tmp` 生成缓存，已停止该次扫描，改为明确检查 `src scripts vite.config.js eslint.config.js`；不把中断的全目录扫描记为通过。
- 当前 Vite 构建：通过，12.45 秒；输出到隔离审计目录，未运行会重写赛事资源的 prebuild。此项是构建检查，不是冻结的生产发布包。
- System 13 项检查：账号认证、Cookie 安全、账号安全、环境门禁、注册关闭、邀请、账号上下文、开放策略、周赛管理权限、比赛准入、路由注册、部署工作流和备份脚本均通过。测试使用夹具，并将数据库连接强制设为回环端口 1，不连接业务库。
- 备份静态检查初次失败是 Windows 的 Python 商店占位程序；仅给该检查进程加入已有捆绑 Python 路径后通过，没有改动系统设置或备份逻辑。
- Stats 正式域名本次 TLS 校验通过；不将此扩展为后端证书、自动续期或长期网络稳定性验收。
- 相关 `git diff --check` 通过。

## 复用但未重跑的证据

同日上一轮本机 API/PostgreSQL 演练已记录：密码重置成功、旧密码被拒绝、新密码可登录、两个原有会话撤销、无关账号会话保留、哈希及版本入库、一次性 token 与过期拒绝。两个合成账号已清理，没有发送真实邮件。

本轮检查时 4467 API 与 55432 数据库未监听，因此没有把该历史结果写成本次重新完成的数据库验收。账号设置上一轮另有真实本机浏览器 → API → PostgreSQL 的资料保存、刷新回读与恢复记录，见 `account-settings-refinement-20260913.md`。

## 建议执行顺序

1. 冻结最新 Stats + System 候选，确认账号首发范围和对应 schema/config；保持邀请制与已确认的竞猜隐藏。
2. 在测试域名联合部署，先验收 `/account` 和 `/api/platform`，再用隔离账号走认领、登录、参赛入口、设备退出、密码恢复；加入实际邮件投递和链接回跳。
3. 若同时开放网页周赛全流程，验收正式周赛配置、代表权限、五图流程及发布周期快照；账号登录可用不能代替这些业务验收。
4. 完成目标备份、升级/恢复与回滚验收后，再进行正式切换。

本轮没有发现需要重新设计账号 UI 的上线阻断；当前主要工作是固定版本、部署衔接和真实运行环境验收。

## 证据目录

`.codex-tmp/account-launch-audit-20260913/`

- `public-probes.json`：正式域名 GET 状态、内容类型、入口资产与 TLS；未认证、未发送业务写入。
- `stats-tests.log`、`source-lint.log`、`build.log`：当前前端检查。
- `backend-checks.json`、各脚本日志及 `check-production-backup-rerun.log`：后端夹具/静态检查与重跑说明。
- `summary.json`：汇总、公开候选差异以及白名单中的非敏感本机配置。
- 上轮真实数据证据：`.codex-tmp/account-entry-20260913/recovery-api-evidence.json`。
