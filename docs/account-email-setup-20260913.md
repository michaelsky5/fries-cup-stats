# 网站发信接入说明

`shenkeyu5@gmail.com` 是这轮指定的测试收件箱。它告诉系统“信寄给谁”，但网站还需要发信服务才能把邮件寄出去。无需把 Gmail 登录密码交给网站或发在聊天里。

## 当前实际状态 · 2026-09-14 02:58 SGT

- 发信服务已接入隔离测试环境，发件人为 `薯条杯 <account@notify.fries-cup.com>`；收件白名单仍仅包含本轮授权邮箱，正式环境未切换。
- 邮件网站入口已改为 https://hub-preview.fries-cup.com，配置切换 `34775122811` 成功，其他容器与数据库 schema 保持不变。
- 本批邀请和找回密码邮件 `34775316045` 均 Delivered、Gmail INBOX，SPF/DKIM/DMARC 通过。实际按钮直达新入口，邀请资料和密码找回表单均已在 Edge 打开。
- 该入口的 18 项真实 HTTP 账号检查通过；用户已亲自设置密码、认领并登录。只读核对 `34776113424` 确认邮箱已验证、原队伍与负责人身份正确，旧邀请预览被 409 拒绝，网页显示“这份邀请已认领”。实际找回密码及找回后的登录尚待完成。旧 Vercel 路由的 TCP 故障根因仍未确定，不将新入口的成功视为旧故障已修复。
- 本批找回密码链接有效至 2026-09-14 03:10（UTC+8）；邀请有效至 2026-09-20 14:58（UTC+8）。密码由用户亲自输入并提交。

## 历史状态 · 2026-09-13 16:08 SGT 后

- 自有通知域名 `notify.fries-cup.com` 已验证。用户批准的四条 DNS 记录已经保存；官网与数据站的路由记录未改。
- 实际测试服务器已部署 `d7dc349`，发件地址为 `薯条杯 <account@notify.fries-cup.com>`，白名单继续限定本轮测试收件箱。正式应用未切换。
- 两封新邀请/找回密码邮件均进入 Gmail 收件箱，认证通过、链接未被改写、UTC+8 时间显示正确。真实脚本验收 `34746919401` 与 Gmail/Resend 记录相互对应。
- 邮件投递已通过这一个指定收件箱的实测；邀请网页仍存在连接超时，实际用户认领、重设密码及后续登录仍未完成。

## 早先状态 · 测试域名发信

- 测试后台：`ACCOUNT_EMAIL_DELIVERY_MODE=resend`，发件地址 `Fries Cup Test <onboarding@resend.dev>`，收件白名单仅为本轮授权邮箱。
- 正式后台：同样未接入发信服务。未在本轮修改正式配置。
- System 工作区及候选 `ac9f812` 已补齐周赛邀请自动发送、统一邮件通道和 Resend 原生适配器；实际测试服务器已部署 `7c4c918`，部署运行 `34743953865` 成功。用户明确授权创建 Sending access 密钥并保存至私有 System 仓库的 GitHub Actions 加密配置。
- 邀请、邮箱验证、找回密码、邮箱变更、申诉和赛事通知共用发送通道；原有 webhook 仍兼容。
- 管理员能看到未启用、模拟、测试收件限制、服务已接收、发送未确认等状态；重新邀请会明确提示旧链接失效。
- 本轮使用真实 HTTP 和独立 PostgreSQL 验证 11 项账号流程，包括邀请邮件失败后的链接有效性、重发后的旧链接失效与新链接认领。另有 6 项发送器测试、生产环境配置校验、lint、构建和后端语法检查通过。
- 真实发送检查 `34744073847` 成功：通过测试服务器的周赛邀请和密码找回 HTTP 接口向指定 Gmail 发送两封邮件，Resend 均显示 Delivered。用户确认两封均收到但在垃圾邮件；Gmail 只读检查也确认 SPAM 标签。
- Gmail 邮件头显示这两封信 SPF、DKIM、DMARC 全部通过。Resend Insights 提醒发件域名与按钮链接域名不一致，这是待改善因素，不能据此认定 Gmail 的唯一判定原因。收件位置验收尚未通过，未主动修改 Gmail 标签。
- Edge 实际打开邮件中的邀请链接后，显示正确测试队伍、受邀身份和设置密码表单；密码找回邮件打开设置新密码弹窗。两者均清除地址栏凭证，尚未替用户设置或重设密码。
- 新增 `624869e` 修正邮件有效期显示为明确的 UTC+8 中文日期，6/6 测试通过并回写工作区；此日期显示修正尚未部署到 `7c4c918`。

## 建议的接入方式

使用事务邮件服务，以“薯条杯”的官方发件地址发送账号邮件。候选服务是 Resend；正式发送前需要开通账号并验证所用发信域名。其测试域名只允许给该 Resend 账号的邮箱发送测试信，不能直接作为正式多用户发信方案。[官方发件地址说明](https://resend.com/docs/knowledge-base/how-do-I-create-an-email-address-or-sender-in-resend)、[测试域名限制](https://resend.com/docs/knowledge-base/403-error-resend-dev-domain)。

发件地址已采用 `薯条杯 <account@notify.fries-cup.com>`，通知子域验证与真实投递测试通过。详见 [DNS 记录与执行范围](account-email-dns-20260913.md)。

用户已完成发信服务登录与本站发信授权；四条 DNS 记录按 Resend 本次实际配置保存并验证。密钥保存在服务端环境与私有 GitHub Actions 加密配置中，未写进 Stats 前端或 Git 源码。

开发侧已完成的部分：

1. 在 System 服务端增加 `ACCOUNT_EMAIL_DELIVERY_MODE=resend`，将邮件内容转换为服务要求的发件人、收件人、主题、HTML 和纯文本格式。[发送接口](https://resend.com/docs/api-reference/emails/send-email)。
2. 发信设置包括 `RESEND_API_KEY`、`ACCOUNT_EMAIL_FROM`、`ACCOUNT_EMAIL_ALLOWED_RECIPIENTS`。示例文件只有空配置，密钥必须保存在服务器配置中。
3. 周赛邀请在数据库提交后发送。邮件失败不撤销邀请；再次邀请生成新 token，使旧 token 失效。相同发送请求使用幂等键；即时账号邮件失败或超时不自动重发，已有赛事通知队列保留使用同一幂等键重试的机制。
4. 邮件使用黑黄白样式、HTML 转义与纯文本备用内容。账号按钮只允许跳转到配置的本站环境，发送日志不包含邮箱、密码或一次性链接。

接下来需要完成：

1. 使用已验收的隔离 EdgeOne 入口继续用户账号路线。旧 Vercel 入口继续保留故障记录；正式选用的部署路径仍需上线验收。
2. 用户认领和登录已通过；继续由用户亲自重设密码，再验证重新登录和旧会话失效，不在聊天里索取密码。
3. 正式上线前统一品牌域名下的网站入口，补齐真实工作人员周赛验收、正式迁移和回退证据。
4. 测试收件白名单继续限制为本轮授权邮箱，正式配置与正式发布另行处理。

自有发件身份、指定 Gmail 收件箱投递与新测试入口已有通过证据；用户设置密码后的完整路线和正式发布验收仍未完成。
