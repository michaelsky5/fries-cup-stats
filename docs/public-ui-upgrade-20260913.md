# 公共 UI 升级 · 2026-09-13

状态：本地实现与代表性浏览器验收完成，未部署。对应[前一轮 UI 审阅](public-ui-audit-20260913.md)，沿用现有 FC Signal 纸色、墨色、黄色、直角及公共导航，不重新定义整体视觉风格。

## 本轮变化

| 页面 | 已实现的调整 |
| --- | --- |
| 周赛总览 | 上区只放焦点比赛与周期积分，本周全部对阵独立通栏；压缩积分说明和关注区间距。手机顺序为焦点、对阵、积分、关注 |
| 我的关注 | 主比赛独占一行，接下来与最近赛果作为下方两个紧凑摘要；保留相关比赛、五局记录、关注和周期数据逻辑 |
| 选手英雄图鉴 | 一位英雄使用横幅，两到三位使用同组展示卡，四到六位采用主卡加单列，其余采用主卡加双列；手机主卡下方使用紧凑次卡或列表。窄屏英文指标可换行 |
| 选手与队伍详情 | 档案、分析、征程的来源说明并入同一赛事状态栏；复用公共黄色整栏页签和分隔线。队伍展厅保留主体叙事构图 |
| 地图与排行控件 | 共用 `SignalSwitch`，保持直角、44px 点击高度、黄色开启态、可见键盘焦点与 `aria-checked`。正式排名开关不生成重复筛选标签 |
| 排行无统计状态 | 赛季完全没有统计时隐藏无效筛选、表格、侧栏和分页，提供赛程和阵容入口；正常筛选导致的空结果保留原有恢复操作 |
| 归档首页文案 | 删除人物区面向设计者的说明；正式排名门槛复用统一资格格式，不再显示旧的 30 MIN |

## 实际浏览器检查

使用本机 Edge，归档预览为 `http://127.0.0.1:3026`，周赛演示为 `http://127.0.0.4:3049`。后者始终显示“设计样例”，对阵、比分、日期、积分不代表真实周赛发布。

| 检查对象 | 实际覆盖 |
| --- | --- |
| 周赛总览 | 中文桌面 1699×935、宽屏 2549×1403，英文手机 390×844；切换第 4 周待赛、从对阵行选择 ECNU–SPC 完赛、查看第 3 周进行中 RR5 |
| 布局测量 | 原 1699px 样例左右差约 223px；调整后焦点区 655.4px、积分区 658.7px，对阵列表独占通栏。2549px 下上区高度相差约 32px |
| 我的关注 | 中文桌面、英文手机，既有三个关注对象；两条摘要在手机各约 178×156px，不随主比赛拉高 |
| 英雄图鉴 | 石头两英雄的中文桌面与手机；雷海棠三英雄英文 980px、700px；LazYYanG 单英雄及 EVER 六英雄桌面。修复 700px 英文卡片说明截断 |
| 详情公共区域 | 石头档案中文桌面、分析英文手机；AIP 征程中文与英文桌面、征程和分析英文手机；来源状态区均为一份，页签切换保持赛季与语言 |
| 共享开关 | 皇家赛道中文桌面使用空格开启、回车关闭，队伍数 34→28→34；选中轨道圆角为 0，焦点可见 |
| 正常排行 | FCR2026 仍显示 286 条职责记录、155 条正式排名；只看正式排名切换前后，表格文档坐标均约 636.6px，无重复条件行 |
| 空统计排行 | FCW2026 中文桌面、英文手机；没有表格或筛选输入，赛程入口实际到达对应周赛赛程 |
| 归档文案 | 人物章节中文桌面及英文手机实际滚动，完整显示 6 图、60 分钟、3 场的本职责门槛 |
| 原版回退 | 石头 `design=original` 桌面可正常显示，未加载新版档案组件 |

所检查页面的外层文档宽度没有超过视口；手机为 382px，常规桌面为 1691px，宽屏为 2541px。临时视口覆盖已恢复。局部图像裁切与内部滚动区不按外层溢出处理。

## 代码与验证边界

- 修改前文件保存在 `tmp/public-ui-upgrade-20260913/before/`，并记录 SHA-256。仅在相关文件上继续修改，保留原有未提交工作。
- 相关 JSX 的 ESLint 通过；`git diff --check` 在仓库现有换行设置下通过。
- 10 个现有检查脚本通过：周赛总览、赛程、晋级、周赛关注、关注信息流、选手档案、地图资料、数据状态、评分资格、评分显示。它们验证既有数据与导航逻辑，不替代视觉验收。
- 使用 Vite 直接构建到独立临时目录，避免 npm 的构建前步骤改写生成资源。最终构建通过。

本轮没有改变评分模型、赛事规则、发布快照或 System。未逐一回放回顾故事、账户、报名和比赛房流程，也未对所有赛季、所有筛选排列作发布验收。回顾入口的文案层级与选手分析长图表的局部密度保留为后续独立精修项。
