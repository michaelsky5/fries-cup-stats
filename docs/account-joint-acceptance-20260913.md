# 账号候选联调验收 · 2026-09-13

本轮范围是冻结 Stats 与 System 候选、部署到隔离测试环境并验收账号路线。当前账号测试入口为 https://hub-preview.fries-cup.com；真实 HTTP 检查 18/18 通过，新邮件送达收件箱。用户已亲自设置密码、完成邀请认领并登录，队伍身份和邮箱验证状态已在数据库核对。实际找回密码与找回后的登录尚待完成；正式站没有切换，不能报告“可以完整上线”。

## 用户认领验收 · 2026-09-14 02:58 SGT

- 用户报告“已绑定”。只读验收 `34776113424` 核对测试邀请在 02:51:23 SGT 持久化为 ACCEPTED，受邀邮箱同刻验证，队伍职务 LEADER 正确关联原账号，MANAGER 身份 ACTIVE。认领后已有一条有效登录，authVersion 为 1。没有读取或提交用户密码。
- 在用户当前 Edge 登录中，账号设置显示正确账号；“我的空间”显示 `真实邮件验收 · 非正式赛事`、MAIL TEST 和队伍负责人，待办同步成功。此测试赛季未配置比赛或周赛周期，页面如实显示等待赛程、没有可操作的周期，未伪造参赛记录。
- 从原邀请链接重新打开，网页明确显示“这份邀请已认领”，不再出现认领密码表单。实际 EdgeOne 预览接口返回 409 / ACCOUNT_LINK_INVITATION_ACCEPTED；后台 acceptedAt 未变。没有尝试使用新密码重复提交认领 POST。
- 本轮没有发送新邮件、修改账号资料或部署应用。只读验收脚本 HEAD 为 `8c3c2429c5c8294ea7094766bd967a9ce971241e`，实际 System 仍为 `d7dc349`。报告位于 `real-mail/human-claim-run-34776113424/account-human-claim-result.json`。
- 02:55:48 SGT 核对最近的找回密码凭证仍未使用，有效至 03:10 SGT。下一步由用户亲自重设密码；重设后先停在重新登录页，以继续检查旧登录失效及新登录。

下节及更早记录保留当时状态；本节更新了实际用户认领这一验收项。

## 最新进展 · 2026-09-14 02:43 SGT

- 复用既有隔离 EdgeOne 项目 `fries-cup-hub-preview`，部署 `dpehz8f73kh4`，Stats 源码 `8e4858993ebfd591c98b0118a40101cd23cd321f`。账号请求固定转发至测试后台，公开赛事数据继续只读。未新增 DNS、主机或付费服务。构建、lint、语法和 diff 检查通过；代理边界测试 11/11 通过。
- 9 月 13 日 16:53 SGT，从该域名进行真实 HTTP 检查 18/18 通过，覆盖登录、资料写入与读回、撤销新建测试会话、当前会话保留、跨站提交拒绝和退出。合成资料已恢复，临时会话已结束；这些是 HTTP 与真实测试库证据，不冒充真人点击验收。
- 9 月 14 日 02:36 SGT，配置切换运行 `34775122811` 成功，仅将测试 `ACCOUNT_PUBLIC_BASE_URL` 改为该域名。继续运行 System `d7dc349` 同一 API 镜像，其他运行中的容器及镜像不变，schema 不变。环境备份与邮件测试凭证保存在服务器私有目录 `shared/account-origin-backups/edgeone-20260913`；早先 `34748742322` 因错误假设容器名称在写入前停止，未造成部分切换。
- 02:40 SGT，单次邮件验收 `34775316045` 成功。经实际 EdgeOne 入口，邀请预览返回 200（762ms），密码找回返回 202（1913ms）并写入一个新的有效凭证。沿用原邀请、原账号和原密码，只向已授权测试邮箱发送邀请与找回邮件各一封。
- Resend 两封均 Delivered；Gmail 两封均 INBOX、无 SPAM，SPF/DKIM/DMARC 通过，按钮直接指向 `hub-preview.fries-cup.com`，未改写为追踪链接。没有更改邮箱标签。详见 `real-mail/edgeone-gmail-authentication.json` 和 `real-mail/edgeone-run-34775316045/account-edgeone-email-result.json`。
- Edge 从本批实际邮件按钮打开了正确的邀请资料和设置密码表单、设置新密码弹窗；地址栏凭证均清除，两个页面的浏览器 warning/error 为空。没有输入或提交新密码，邀请在发信验收时为 PENDING。密码找回邮件本次有效至 03:10 SGT，过期后应通过网页重新申请，不能把旧链接当作可继续使用的入口。
- Vercel 旧入口的间歇 TCP 失败根因仍未定位。16:34–16:35 SGT 的只读观测 `34748006304` 仅捕获成功请求，不能证明旧入口的连接问题已经解决；防火墙未修改。此次通过既有 EdgeOne 入口完成测试，短时通过不代表持续可用性或正式上线批准。
- 前端回退包为 `Z:/FriesCup/fries-cup-stats-migrations/20260905-edgeone/edgeone-preview.zip`，SHA256 `fed3c9f3283367449e72850810fd9f20907b7b0f0984c6402ad93b110906209f`，包内版本与原部署 `dp4cxvg1s7rw` 一致。新包与部署证据在 `.codex-tmp/account-joint-20260913/edgeone/`。需要回退时必须同时审阅账号邮件入口配置；仅回退静态文件不等于恢复账号流程。

下一步由用户亲自设置密码并确认身份，再检查认领后的空间、重复认领拒绝、找回后的重新登录与旧会话失效。测试白名单保持单邮箱；真实工作人员周赛验收、正式迁移和回退、正式发信配置仍分别保留为上线条件。下面的历史记录描述当时状态，不覆盖本节。

## 历史进展 · 2026-09-13 16:08 SGT 后

- 用户明确批准为 `notify.fries-cup.com` 保存四条发信 DNS 记录并继续测试。DNSPod 从 21 条增至 25 条；权威 DNS 值核对通过，Resend 于 16:01 SGT 显示 Verified。只新增通知子域记录，官网与数据站的路由记录未改。
- 测试部署 `34746818258` 成功，实际运行 `d7dc349fede17139278eb47d74ebf6ff88df4726`；测试发件人改为 `薯条杯 <account@notify.fries-cup.com>`，收件人白名单保持一个，公共注册与 token 暴露继续关闭。数据库、环境、旧镜像和合成邮件验收访问文件已备份，正式 API/Admin 容器及镜像未变。
- 真实复测 `34746919401` 成功，沿用现有合成账号、保留邀请凭证与密码。Gmail 两封新邮件均有 INBOX、无 SPAM，SPF/DKIM/DMARC 均通过；CTA 保持直达原预览地址，无追踪重定向，日期显示 UTC+8。
- 同一推送的另一条运行 `34746919416` 因已有单次标记被拒绝，未重复发送。没有删除标记后重试。后续 `e872a21` 和 `2ecbe2e` 仅调整验收/只读诊断，未重新部署应用。
- 新邮件的密码找回弹窗可以打开；用户密码尚未输入或提交。实际邀请网页两次读取失败，均返回 `ACCOUNT_PROXY_CONNECT_TIMEOUT`，诊断出口 `13.212.218.90`；这与真实邮件的投递结果分开记。记录详情见 `account-email-dns-20260913.md`。
- 16:24 SGT 第三次邀请读取仍超时，诊断出口变为 `13.229.100.211`。只读检查排除了旧诊断 IP 在一个托管黑名单中的匹配；45 秒观测仍限定旧 IP，不能将没有匹配包解释为新请求未到达服务器。诊断运行 `34747166764`、`34747559827` 成功执行，应用运行版本未变，未调整防火墙。只读诊断候选 HEAD 为 `42673b1`。

以下 15:00、13:48 的记录保留当时状态；当前以本节及发信域名验收记录为准。

## 真实邮件续作 · 15:00 SGT 后

- 用户在 Edge 登录 Resend，并明确授权创建仅 Sending access 的密钥，保存至私有 System 仓库的 `STAGING_RESEND_API_KEY` 加密配置。
- 部署 `34743953865` 成功，实际 System 测试版为 `7c4c918e5c73f129578c57435d9df182f00af0c9`。只启用测试服务器的 Resend 通道；收件白名单仅本轮授权邮箱。已有数据库和配置备份、旧镜像标签已保留，健康检查通过，正式 API/Admin 容器与镜像 ID 在本轮前后保持一致。未执行数据库 schema 更新。
- 实际 HTTP 发信验收 `34744073847` 成功：限定新建 `JOINTEMAIL20260913` 测试赛季、拒绝覆盖已有收件账号，调用周赛邀请与密码找回接口。邀请 Resend ID `dfb178b3-916e-4b34-8cbe-894d5b9f4bd1`；密码找回 ID `b663f47b-ac22-4aa9-9fff-31cee4d2efd7`。
- 两封邮件均显示 Delivered；用户与 Gmail 只读检查共同确认邮件进入 SPAM。两封信的 SPF、DKIM、DMARC 都通过；Resend Insights 提醒发件域名与链接域名不同，垃圾邮件判定的具体原因未确证。未改变 Gmail 标签。
- Edge 从实际发出的链接打开邀请表单，显示正确测试队伍、身份、掩码邮箱和密码设置入口；密码找回链接打开设置新密码弹窗。没有代替用户提交新密码，实际认领与找回后的登录验收仍待继续。
- 专用通知域名已添加到 Resend，仍未验证。用户已完成 DNSPod 登录；浏览器短暂中断后恢复，已核对全部 21 条现有记录，四条通知子域记录已填好，停在最终提交前等待具体发信授权确认，DNS 尚未写入。本地已准备更换发件地址的部署预检和合成邮件验收数据保留步骤，未推送或部署。详见 `account-email-dns-20260913.md`。
- 邮件中非整秒 UTC 时间直接显示的问题已在代码 `624869e` 修正，6/6 邮件测试通过；此显示修正未部署，实际服务仍为 `7c4c918`。

以下“13:48 续作”和初始记录保留当时状态，当前状态以上述真实邮件续作为准。

## 续作进展 · 13:48 SGT

- Stats 当前隔离测试版：`df4f0d279795b02c743551146442339f8ff3b49a`，部署 `dpl_hsSgv3SHgmaZZTw48rsodDZWLXCV`，READY。资料保存验收先在前一版 `f1d7b6e` 完成；后续版本仅增加测试环境连接诊断。
- System 邮件代码候选：`ac9f812`。实际测试服务继续运行 `48d625800501798ece893c7c68377ce2100e8a14`；本轮邮件代码未部署启用，正式环境未切换。
- 已补齐并回写工作区：Resend 原生发信适配器、邀请/验证/密码找回等黑黄白 HTML 与纯文本邮件、测试收件白名单、邀请投递状态、重新邀请时旧链接失效，以及个人介绍 280 字限制与后台一致。
- 邮件验收：发送器 6/6、生产配置校验、账号安全和邀请绑定回归通过；真实 HTTP + 独立 PostgreSQL 11/11，包括邮件失败不撤销有效邀请、重新邀请后旧链接失效及新链接可认领。报告在本机临时目录 `friescup-registration-postgres-FeEEfz`。仅使用本机模拟收件端；无真实邮件。
- System 前端构建及 328 个后端文件语法检查通过，改动文件 lint 和 diff 检查通过。Stats 账号设置和账号资料契约检查通过。

### 连接排查和证据边界

1. 有效预览凭证下，经同一入口登录和退出成功，资料与设备读取各超时 22 秒。不能再把这两次超时仅归于缺少预览登录权限；证据 `preview-auth-timing.json`。
2. 只读服务器诊断 `34739594234`、`34740194020` 显示，失败请求未到达 Nginx/账号 API；同期直连检查与 GitHub runner 的测试 API 检查通过。未修改服务器防火墙、证书或正式代理配置。
3. 在隔离 Stats 候选增加带请求 ID 的私有账号转发，保留应用 cookie、Origin 校验和 TLS，禁止缓存、自动跟随跳转与已提交写操作的重放。边界测试 8/8；未建立 TCP 连接时的有限重连测试 3/3。
4. 香港节点和新加坡节点均出现过解析到正确 IPv4 后无法建立 TCP 连接的情况。日志能将失败缩小到建立连接阶段，但尚未确定上游网络、云平台出口或服务器入口中的最终责任点。复用连接和切换地区没有消除所有失败。
5. 13:45 SGT，新加坡测试版实际浏览器完成个人介绍保存，接口返回 200；刷新后页面显示保存后的内容。运行日志请求 `ea3d15bc-cc93-4a5c-bd9b-b07942b4d1ab` 是 PATCH 成功，`6b88c073-1351-4bbc-8129-d0b90e2d6fa5` 是刷新读取成功。同次刷新也有一条额外读取连接失败，因此不能据此宣称链路稳定。
6. 13:47 SGT，进入设备页再次连接超时，页面明确显示“设备列表暂未同步”，提供“刷新设备列表”，没有展示虚假的有效设备数。点击恢复仍失败；该失败记录保留。
7. 14:01–14:03 SGT，后续测试版实际网页完成“退出其他所有设备”：确认弹窗说明结束一条其他登录，提交成功后从两条变为一条，当前登录保留。刷新页面仍为一条；对应 POST 请求 `20a16c2a-4e7d-45a9-991f-6cfe1c0a6a01` 和刷新 GET 请求 `8810b41c-95f7-495c-8a3c-77b0d06d3bda` 均为 200。至此线上资料保存、刷新和设备撤销的功能用例均有通过证据，间歇连接失败的稳定性问题仍未关闭。
8. 只读主机检查 `34741371118`、`34741463975` 显示连接追踪使用 86/65536，未接近上限；主机存在托管安全规则 `YJ-FIREWALL-INPUT` 和 `YJ-GLOBAL-INBLOCK`，后者包含 3640 个地址并有拦截计数。但没有把失败请求的来源与封禁列表匹配，因此不能认定误封是根因。防火墙未修改。
9. 测试分支可在建立连接失败时单独查询服务器出口 IP，以便后续核对封禁；不会携带账号 cookie、密码、邮箱或业务请求内容。最后一版目前记录的操作都成功，尚无新的失败出口样本。此诊断只用于隔离候选，不能原样视作正式发布配置。

账号转发和地区实验只留在隔离候选分支，未接入主工作区的正式 `vercel.json`。个人介绍限制和邮件功能已通过聚焦补丁回写主工作区，保留原有未提交改动。下一步先关闭线上连接稳定性问题，再启用测试发信；真实工作人员周赛验收、正式迁移与回退验收仍独立保留。设备页目前记录的 IP 是代理出口，不能用来证明用户真实来源；正式显示该字段前还需明确可信代理链路。

## 固定候选

- Stats：`554d27afb2a74bfb0b3598011ec5414cab0ca9db`，分支 `codex/stats-account-joint-20260913`。
- System 修复候选：`48d625800501798ece893c7c68377ce2100e8a14`，分支 `codex/system-account-joint-20260913`。
- 两个候选均在独立 worktree；原工作区未 reset、clean 或整体暂存。
- Stats 预览部署 `dpl_HDf9H8qteZJxqocT1suenj9oRrf3` 已 READY，账号 API 仅转发到 `test-admin.fries-cup.com`。
- 输入清单、摘要和日志在 `.codex-tmp/account-joint-20260913`。字体授权原文中的空白被保留；候选里 11 个源码文件只做了尾随空白规范化。

## 本轮实际修复

1. 两处源码检查依赖固定中文 JSX 字面值，在国际化后误报；改为允许对应可访问名称的翻译表达式，以及检查 h2/h1 的正确层级。
2. ESLint 排除本机 `.codex-tmp` 构建产物，避免把压缩 bundle 当源码检查。
3. 测试部署发现后端选图模块导入了未打入 API 镜像的前端地图文件。把地图常量移到 `shared/mapPool.js`，前端原路径保留 re-export，后端直接导入 shared。
4. API Docker 构建现在实际导入打包后的服务模块，提前发现丢失依赖。
5. 联调部署脚本在健康检查失败时显式恢复旧应用镜像及测试环境配置；正式 Compose、代理、DNS 不在本轮变更中。

## 当前证据

| 层次 | 本轮证据 | 边界 |
| --- | --- | --- |
| Stats | 完整 `test:account-ui` 通过，lint 0 errors / 18 warnings，隔离构建通过 | warnings 是已有 Hook 依赖提示；不代表每个公网数据页面都重新人工验收 |
| System | lint 通过；前端构建通过；API 模块导入通过 | Docker 里的模块导入在实际测试部署中另行检查 |
| 真实 PostgreSQL | 报名 17/17、先手与比赛房 57/57、协调 7/7、比赛控制 27/27 | 每套使用自己创建的临时库；不是生产数据迁移 |
| 账号 HTTP + DB | 新增 9/9：邀请一次性认领、邀请制、身份与赛季发现、资料保存、设备撤销、改密、找回密码、邮箱验证、发送失败 | 实际 loopback HTTP，不是 Fastify inject；邮件使用 loopback 模拟收件端 |
| 打包修复回归 | 先手模式相关真实 DB 检查 6/6；前端构建产物 hash 保持一致 | 地图内容未改，只调整共享依赖位置 |
| 本机浏览器 | 邀请信息加载后 URL 清除 token；登录弹窗；受邀赛事进入我的空间；设置页编辑保存、刷新后保留 | 合成账号，真实 API 与 DB |
| 线上浏览器 | 登录成功、身份正确；设备列表能读取；390px 窄屏宽度 382/382，无外层横向溢出 | 验收未通过：首次设备读取超时，撤销其他设备请求超时且刷新后仍有原会话，个人资料读取重试后仍报服务不可用；控制台无 warning/error 不能代替这些实际失败 |
| 直接访问测试 API | 本机通过 HTTPS 登录、读取设备及资料、退出本次临时会话均返回 200；设备与资料读取分别 321ms / 368ms | 这一次直连成功不证明代理链路稳定，也没有确定代理、区域网络或源站的具体根因 |

旧 `run-account-e2e-rehearsal.js` 仍依赖未发布的 PredictionPolicy 等 WIP schema，尝试运行后在创建的临时库失败并清理。没有把它算作通过，也没有为过时测试加入竞猜表。当前邀请制账号由新增的真实 HTTP + PostgreSQL 检查覆盖；正式数据库迁移仍需单独的生产影子验收。

## 测试部署与回退记录

- 首次 System 联调部署：`34738177909`。测试 schema 同步成功；API 镜像因缺少地图文件未启动，发布未完成。
- 已实际执行测试应用恢复：`34738388959` 成功，测试 API 与 DB 健康；正式 API/Admin 的容器及镜像 ID 与部署前一致。
- 修复后部署：`34738464964` 成功，实际运行 SHA 为 `48d625800501798ece893c7c68377ce2100e8a14`。镜像模块加载、API/DB、管理端和测试域名健康检查通过；正式容器及镜像 ID 再次核对一致。
- 线上接口验收：`34738613037` 成功。限定新建合成赛季的账号，验证邀请创建/认领/重复拒绝、cookie 登录、赛季身份与空间、资料保存、其他设备撤销、无登录及不受信任 Origin 拒绝。访问资料保存在本机 `.codex-tmp/account-joint-20260913/online-access/`，不写进正式用户资料。
- 12:45–12:46 SGT 从本机验证：测试 `health`、测试 `auth/config`、正式 `health` 都是有效 TLS 下的 200 JSON；测试自助注册和发信继续关闭。
- 测试库的 dump、SHA256、旧环境文件和旧镜像已保存在服务器隔离测试备份目录。没有清除旧测试数据，也没有恢复或迁移正式数据库。

## 线上浏览器未通过项

- 12:49–12:58 SGT，设备列表第一次读取提示超时，进入设备页后成功显示两条合成账号会话。
- 点击“退出其他所有设备”并确认后，15 秒超时保护提示提交结果未确认。仅刷新状态，没有盲目重发写请求；刷新后原先两条会话仍在，不能记为撤销成功。
- 个人资料页读取提示服务不可用，点击页面提供的“重新读取资料”后仍失败。因未读到表单，线上浏览器资料保存没有执行。
- 使用相同合成账号、相同 Origin，直接经测试 HTTPS API 请求登录、设备、资料、退出都成功。证据见 `direct-account-timing.json`；只支持“页面链路与直连接口表现不同”，根因尚未确认。
- 另一次命令行访问受保护的 Vercel 预览被转到 Vercel HTML 页面，不能把其中的 200 当作账号 API 成功；`preview-network-timing.json` 已记录 finalHost/contentType。
- 同一浏览器标签直接打开 `/api/platform/me/profile` 曾被转到 Vercel 登录页；更新临时预览访问凭证后，页面资料请求仍超时，直接打开接口又得到浏览器 `net::ERR_BLOCKED_BY_CLIENT`。因此存在预览访问保护与浏览器阻止访问的因素，不能直接归因为账号代码故障或源站网络问题。本轮没有关闭访问保护或绕过浏览器限制。
- 本轮最后已恢复浏览器常规尺寸，保留测试账号概览标签。浏览器合成账号有验证标记来自邀请认领验收，不表示真实 Gmail 邮件已收到。

## 剩余上线条件

1. 定位并解决线上页面到账号后台的超时与读取失败，再完成线上资料保存、设备撤销及刷新持久化验收。服务内部接口通过不替代浏览器闭环。
2. 实际工作人员用完整周赛资料做首轮使用验收；当前新增赛季和用户均为合成验收资料。
3. 发信和真实链接入口已通，但邮件进入垃圾箱。完成正式通知子域验证、品牌网页入口和自然投递复测，并由用户设置密码后继续认领/恢复登录验收。详见 `account-email-setup-20260913.md`。
4. 对正式数据库取新备份、完成候选迁移与旧版本回退演练，并审阅正式 Compose/环境变更路径。测试环境的 `db:push` 不替代这个步骤。
